
public class Warrior2 {
	/*
    // Constructor with only 2 of the parameters given.
    public Warrior(String place, int health)
    {
	healthLevel = health;
	if(health > 0)
		alive = true;
	else
		alive = true;
	placeOfOrigin = place;
	personalDescription = "Generic Warrior";
    }


    // Constructor with only 2 of the parameters given.
    public Warrior(int health, String desc)
    {
	healthLevel = health;
	if(health > 0)
		alive = true;
	else
		alive = true;
	placeOfOrigin = "Parts Unknown";
	personalDescription = desc;
    }


    // Constructor with only 2 of the parameters given.
    public Warrior(String place, String desc)
    {
	healthLevel = 100;
	alive = true;
	placeOfOrigin = place;
	personalDescription = desc;
    }


    // Constructor with only personalDescription of the Warrior given.
    public Warrior(String desc)
    {
	healthLevel = 100;
	alive = true;
	placeOfOrigin = "Parts Unknown";
	personalDescription = desc;
    }

    // Constructor with only placeOfOrigin given.
    public Warrior(String place)
    {
	healthLevel = 100;
	alive = TRUE;
	placeOfOrigin = place;
	personalDescription = "Generic Warrior";
    }


    // Constructor with only the healthLevel given
    public Warrior(int health)
    {
	healthLevel = health;
	if(health > 0)
	    alive = true;
	else
	    alive = false;
	placeOfOrigin = "Parts Unknown";
	personalDescription = "Generic Warrior";
    }

*/
}
